"""shrike — the autonomous security agent for your AI stack.

Discovers the MCP servers and agent tools your assistants are wired to, finds the
vulnerabilities, reasons about blast radius, and writes the fixes — on a local model,
so nothing leaves your machine.
"""
__version__ = "1.0.0"
